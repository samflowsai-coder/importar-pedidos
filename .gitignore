.env
.env.*
!.env.example
config.json
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
dist/
build/
*.egg-info/
.DS_Store
.idea/
.vscode/
.claude/
.superpowers/
input/
output/
logs/
# Business documents (root-level only)
PRD_Automacao_Pedidos.pdf

# Firebird databases & backups (NEVER commit production data)
*.fdb
*.fbk
*.gbk
jaybird.jar
backup Fire/
bkp Fire/
bkp fire/
bkp Fire Novo/
schema_report.txt

# Local SQLite log
*.sqlite
*.sqlite3
*.db

# Plan files (ad-hoc); excecao: docs/superpowers/plans/ é documentação
plans/
!docs/superpowers/plans/

# Local UI-editable config + Fernet key (NEVER commit)
app/firebird.json
app/.secret.key

# Bat com token de PROD embutido (client-specific) — nunca commitar
promover-prod.bat
ligar-flowpcp.bat
enviar-catalogo-flow.bat

# Dados do cliente / produção — nunca commitar (PDFs, dbs, patches gerados)
ambiente_cliente/
reprocessar_prazos_patch.*
wheelhouse/

# SQLite WAL/SHM (o *.db já é ignorado; estes sufixos escapavam)
*.db-shm
*.db-wal
*.db-journal
# Logs e artefatos transitórios de dev
pip-log.txt
# Planilhas de análise pontual na raiz (não são fixtures)
/comparacao_catalogo_confeccao_x_americanense.xlsx
# Amostras de debug ad-hoc (não são fixtures do repo)
samples/Erro Magic Feet/

# Saidas dos scripts de diagnostico (geradas no servidor do cliente)
app-boot-out.txt
app-boot-err.txt

# Corpo do GitHub Release (gerado pelo workflow de release)
release_body.md
