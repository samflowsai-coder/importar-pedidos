# Módulo: validators

Arquivo: `app/validators/order_validator.py`. Garante campos obrigatórios e `quantity > 0`. Levanta `ValueError` em violação.

Sem teste isolado — cobertura indireta via testes de parsers.
